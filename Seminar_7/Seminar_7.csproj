/*
// Задача 47. Задайте двумерный массив размером m×n, 
// заполненный случайными вещественными числами.
// m = 3, n = 4.
// 0,5 7 -2 -0,2
// 1 -3,3 8 -9,9
// 8 7,8 -7,1 9

double [,] matrix = new double [3,4];

for(int i = 0;i < 3; i++)
{
    for(int j = 0; j < 4; j++)
   {
   matrix[i,j] = new Random(). Next(-9,10) + new Random().Nextdouble();
   matrix[i,j] = Math.Round(matrix[i,j], 1);

   Console.Write(matrix[i,j]+"\t");
   }
   Console.Write();
 }
 */

 /*

//  Задача 50. Напишите программу, которая на вход принимает позиции элемента в двумерном массиве, и возвращает значение этого элемента или же указание, что такого элемента нет.
// Например, задан массив:
// 1 4 7 2
// 5 9 2 3
// 8 4 2 4
// 17 -> такого числа в массиве нет  

Console.WriteLine("Enter number of rows :");
int m = Convert.ToInt32(Console.ReadLine());
Console.WriteLine("Enter number of colums :");
int n = Convert.ToInt32(Console.ReadLine());

int[,] matrix = new int [m,n];

for(int i = 0; i < matrix.GetLenght(0); i++)
{
    for(int j = 0; j < matrix.GetLenght(1); j++)
    {
        matrix[i,j] = new Random().Next(0,21);
        Console.Write(matrix[i,j]+"\t");

    }
    Console.Write();
}

Console.WtiteLine("Enter row:");
int y = Convert.ToInt32(Console.ReadLine());
Console.WtiteLine("Enter column:");
int x = Convert.ToInt32(Console.ReadLine());
if ((y>m)||(x>n))
{
  Console.WtiteLine("There's no number.");  
}
else
{
    Console.WtiteLine("There's number :"+ matrix[y,x]);
}
*/

 /*
Задача 52. Задайте двумерный массив из целых чисел.
Найдите среднее арифметическое элементов в каждом столбце.
Например, задан массив:
1 4 7 2
5 9 2 3
8 4 2 4
Среднее арифметическое каждого столбца: 4,6; 5,6; 3,6; 3.

Console.WriteLine("Enter number of rows :");
int m = Convert.ToInt32(Console.ReadLine());
Console.WriteLine("Enter number of colums :");
int n = Convert.ToInt32(Console.ReadLine());
 int[,] matrix = new int [m,n];

for(int i = 0; i < matrix.GetLenght(0); i++)
{
    for(int j = 0; j < matrix.GetLenght(1); j++)
    {
        matrix[i,j] = new Random().Next(0,11);
        Console.Write(matrix[i,j]+"\t");
}
    Console.Write();
}
double [] sum + new double[matrix.GetLenght(0)];
for (int j = 0; j < matrix.GetLenght(0); j++)
    {

    sum[j]=0;
    for (int i = 0; i < matrix.GetLenght(1); i ++)
    {
        sum[j]=0;
        for (int i=0; i < matrix.GetLenght(1); i++)
        {
            sum[j]+= matrix[i,j];
        }
        Console.WriteLine($"{j} avarage is : {sum[j]/m}");
 }
 */